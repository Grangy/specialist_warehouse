# 🔒 Аудит безопасности - Файлы с чувствительными данными

## ✅ Файлы, которые УЖЕ правильно игнорируются (.gitignore):
- `.env` - локальные переменные окружения
- `.env.local` - локальные переменные окружения
- `*.db`, `*.sqlite` - базы данных
- `*.log` - логи
- `node_modules/` - зависимости

## ⚠️ Файлы с чувствительными данными в Git:

### 1. **Пароли в примерах кода** (низкий риск, но лучше исправить):
- `scripts/API-AUTH-EXAMPLES.md` - содержит `"password": "YOUR_PASSWORD"`
- `scripts/curl-custom-order-examples.md` - содержит `"password": "YOUR_PASSWORD"`
- `scripts/curl-example-with-auth.sh` - содержит `PASSWORD="${ADMIN_PASSWORD:-YOUR_PASSWORD}"`
- `scripts/curl-create-custom-order.sh` - может содержать пароли
- `prisma/seed.ts` - содержит пароли для seed данных: `YOUR_PASSWORD`, `collector123`, `checker123`

**Рекомендация**: Заменить на плейсхолдеры типа `YOUR_PASSWORD` или использовать переменные окружения.

### 2. **IP адреса серверов** (средний риск):
- Множество файлов в `docs/` и `scripts/` содержат реальный IP: `localhost`
- `scripts/curl-example-with-auth.sh` - содержит `BASE_URL="http://localhost:3000"`

**Рекомендация**: Заменить на `localhost:3000` или использовать переменные окружения.

### 3. **Файлы, которые могут содержать реальные данные**:
- `scripts/test-1c-data.ts` - тестовые данные для 1C
- `scripts/API-AUTH-EXAMPLES.md` - примеры с реальными данными

## 📋 Рекомендации:

1. **Заменить хардкод паролей на плейсхолдеры** в примерах документации
2. **Заменить реальные IP адреса** на `localhost` или переменные окружения
3. **Проверить**, нет ли реальных production данных в тестовых скриптах
4. **Добавить в .gitignore** (если нужно):
   - `*.local.env`
   - `config.local.*`
   - `secrets/`

## ✅ Что уже хорошо:
- `.env` файлы правильно игнорируются
- Базы данных не коммитятся
- Логи не коммитятся
- `node_modules` игнорируется
