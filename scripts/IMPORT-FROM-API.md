# Импорт данных из API в локальную БД

Скрипт для импорта данных с сервера в локальную базу данных через API.

## Возможности

- ✅ **Безопасный импорт** - только обновление/добавление данных, без удаления
- ✅ **Без дублей** - использует `upsert` по уникальным полям
- ✅ **Пакетная обработка** - обрабатывает данные пакетами для производительности
- ✅ **Многопоточность** - параллельная обработка пакетов
- ✅ **Логирование** - подробное логирование прогресса
- ✅ **Тестовый режим** - возможность протестировать на 10 записях каждого типа

## Что импортируется

1. **Пользователи (Users)**
   - Обновляет существующих пользователей по `login`
   - Создает новых пользователей (с временным паролем)
   - ⚠️ Пароли НЕ импортируются (не экспортируются через API)

2. **Регионы и приоритеты (RegionPriorities)**
   - Обновляет/создает приоритеты регионов по дням недели
   - Использует уникальное поле `region`

3. **Настройки системы (SystemSettings)**
   - Обновляет/создает системные настройки
   - Использует уникальное поле `key`

4. **Заказы (Shipments)**
   - Обновляет/создает заказы по номеру (`number`)
   - Импортирует позиции заказов (ShipmentLines)
   - Импортирует задания (ShipmentTasks)
   - Импортирует позиции заданий (ShipmentTaskLines)
   - Связывает с пользователями по логину

## Использование

### Базовое использование

```bash
npx tsx scripts/import-data-from-api.ts \
  --url https://sklad.specialist82.pro \
  --login admin \
  --password YOUR_PASSWORD
```

### Тестовый режим (10 записей каждого типа)

```bash
npx tsx scripts/import-data-from-api.ts \
  --url https://sklad.specialist82.pro \
  --login admin \
  --password YOUR_PASSWORD \
  --test
```

### Импорт только определенных типов данных

```bash
# Только пользователи и регионы
npx tsx scripts/import-data-from-api.ts \
  --url https://sklad.specialist82.pro \
  --login admin \
  --password YOUR_PASSWORD \
  --skip-shipments \
  --skip-settings

# Только заказы
npx tsx scripts/import-data-from-api.ts \
  --url https://sklad.specialist82.pro \
  --login admin \
  --password YOUR_PASSWORD \
  --skip-users \
  --skip-regions \
  --skip-settings
```

### Настройка размера пакета

```bash
npx tsx scripts/import-data-from-api.ts \
  --url https://sklad.specialist82.pro \
  --login admin \
  --password YOUR_PASSWORD \
  --batch-size 10
```

## Опции

- `--url <URL>` - URL сервера (обязательно)
- `--login <LOGIN>` - Логин для авторизации (обязательно)
- `--password <PASSWORD>` - Пароль для авторизации (обязательно)
- `--test` - Тестовый режим (только 10 записей каждого типа)
- `--batch-size <N>` - Размер пакета для параллельной обработки (по умолчанию 5)
- `--skip-users` - Пропустить импорт пользователей
- `--skip-shipments` - Пропустить импорт заказов
- `--skip-regions` - Пропустить импорт регионов
- `--skip-settings` - Пропустить импорт настроек

## Безопасность

- ✅ **Не удаляет данные** - только обновляет/добавляет
- ✅ **Использует upsert** - избегает дублей по уникальным полям
- ✅ **Не перезаписывает флаги** - не обновляет `deleted`, `exportedTo1C` и другие критичные флаги
- ✅ **Временные пароли** - новые пользователи создаются с временными паролями (нужно сбросить)
- ✅ **Валидация данных** - все данные валидируются перед импортом
- ✅ **Обработка ошибок** - автоматические повторные попытки при ошибках сети
- ✅ **Синхронизация статусов** - опция `--force-processed` для безопасной синхронизации обработанных заказов

## 10 улучшений в новой версии

1. **Надежная авторизация** - повторные попытки при ошибках авторизации
2. **Улучшенные запросы** - обработка редиректов и автоматическая переавторизация
3. **Валидация пользователей** - проверка данных перед импортом
4. **Валидация регионов** - проверка данных регионов
5. **Безопасный импорт настроек** - валидация ключей и значений
6. **Безопасная синхронизация статусов** - опция `--force-processed` для синхронизации обработанных заказов
7. **Надежный импорт позиций** - обработка ошибок для каждой позиции
8. **Надежный импорт заданий** - синхронизация статусов заданий
9. **Валидация позиций заданий** - проверка данных перед импортом
10. **Подробная статистика** - метрики производительности, время выполнения, количество запросов

## Важные замечания

1. **Пароли пользователей** - не импортируются, так как не экспортируются через API. Новые пользователи создаются с временными паролями.

2. **ID записей** - могут отличаться между сервером и локальной БД. Скрипт использует уникальные поля (`number`, `login`, `region`, `key`) для сопоставления.

3. **Связи** - пользователи связываются по логину, позиции заданий - по SKU и shipmentId.

4. **Производительность** - для больших объемов данных рекомендуется использовать `--batch-size` для настройки параллелизма.

## Примеры вывода

```
🚀 Начинаем импорт данных с https://sklad.specialist82.pro
📊 Режим: ТЕСТОВЫЙ (10 записей каждого типа)
📦 Размер пакета: 5

🔐 Авторизация...
  ✓ Авторизация успешна

👥 Импорт пользователей...
  Найдено 15 пользователей, импортируем 10
  Прогресс: 10/10
  ✓ Импортировано: 0, Обновлено: 10

🗺️  Импорт регионов и приоритетов...
  Найдено 22 приоритетов, импортируем 10
  Прогресс: 10/10
  ✓ Импортировано: 0, Обновлено: 10

⚙️  Импорт настроек системы...
  Найдено 2 настроек
  ✓ Импортировано: 0, Обновлено: 2

📦 Импорт заказов...
  Найдено 306 уникальных заказов, импортируем 10
  Прогресс: 10/10 (Импортировано: 0, Обновлено: 10, Ошибок: 0)
  ✓ Импортировано: 0, Обновлено: 10, Ошибок: 0

============================================================
📊 ИТОГОВАЯ СТАТИСТИКА ИМПОРТА:
============================================================
👥 Пользователи: Импортировано 0, Обновлено 10
🗺️  Регионы: Импортировано 0, Обновлено 10
⚙️  Настройки: Импортировано 0, Обновлено 2
📦 Заказы: Импортировано 0, Обновлено 10, Ошибок 0

✅ Импорт завершен успешно!
```

## Требования

- Node.js
- Prisma Client (генерируется через `npx prisma generate`)
- Доступ к серверу через API
- Локальная база данных (prisma/dev.db)
