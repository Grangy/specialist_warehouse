/**
 * Минимальная ставка доп. работы (баллов за час) в рабочее время.
 * Используется в формуле (сервер) и в UI без тяжёлых импортов.
 */
export const MIN_EXTRA_WORK_RATE_PER_HOUR = 52;

/**
 * Если за последние 15 мин нет складского темпа (нет отметок), раньше ставка была 0 —
 * доп.работа в «тишине» недоначислялась. Берём эту долю от минимальной ставки за минуту.
 */
export const EXTRA_WORK_PACE_EMPTY_RATE_FRACTION = 0.62;

/**
 * Нижняя граница веса распределения доп.работы (доля от топ-1 месяца): max(FLOOR, √(baseProd/baseTop1)).
 * Вынесено сюда, чтобы клиентский UI мог импортировать без prisma.
 */
export const EXTRA_WORK_WEIGHT_FLOOR = 0.5;
