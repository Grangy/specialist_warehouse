# Database
# SQLite database connection string
# The database file will be created at: prisma/dev.db
DATABASE_URL="file:./prisma/dev.db"

# API Base URL (optional, defaults to localhost:3000)
# API_BASE="http://localhost:3000/api"

# Yandex OAuth (для yandex.js — загрузка бэкапов на Яндекс.Диск)
# Получить: https://oauth.yandex.ru/
# YANDEX_CLIENT_ID=...
# YANDEX_CLIENT_SECRET=...
# YANDEX_REDIRECT_URI=https://oauth.yandex.ru/verification_code

# Cookie Security Settings (для PWA в HTTP окружении)
# Если PWA работает через HTTP (например, локальная сеть), установите:
# NEXT_PUBLIC_DISABLE_SECURE_COOKIE=true
# Это позволит устанавливать cookies без HTTPS
# 
# Если нужно принудительно включить secure cookies (требует HTTPS):
# NEXT_PUBLIC_FORCE_SECURE_COOKIE=true

